version: '3.8'
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.api
    ports:
      - "8000:8000"
    environment:
      - ENV=development
    volumes:
      - ./backend:/app

